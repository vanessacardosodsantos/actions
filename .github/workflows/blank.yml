name: Exemplo de fluxo de trabalho 

on:
  workflow_dispatch:

jobs:
  acao_inicial_job:
    runs-on: ubuntu-latest
    steps:
      - name: Impreme OLÁ
        run: echo "OLÁ"
      - name: Imprime TCHAU
        run: echo "Tchau"

